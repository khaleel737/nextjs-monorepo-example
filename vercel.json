{
  "buildCommand": "pnpm turbo run build",
  "outputDirectory": "apps/main/.next",
  "installCommand": "pnpm config set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
