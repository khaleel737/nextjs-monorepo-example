{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=main...",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/main/:path*",
      "destination": "/:path*"
    }
  ]
}
