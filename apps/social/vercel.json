{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=social...",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/social/:path*",
      "destination": "/:path*"
    }
  ]
}
